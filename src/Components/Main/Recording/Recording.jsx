import React from "react";

const Recording = (props) => {
    
    return (
        <div>
            <div>
                {props.Recording.selectedDay}
            </div>
        </div>
    );
}

export default Recording;